---
const {name, rol, message} = Astro.props
---

<div class="flex gap-2 items-center my-3">
    <picture>
        <div class="bg-gray-300 min-w-[50px] min-h-[50px] max-w-[50px] max-h-[50px] rounded-full"></div>
    </picture>
    <div class="flex flex-col items-start justify-start">
        <span class="font-bold text-lg leading-tight">{name}</span>
        <span class="text-gray-500 text-xs leading-tight font-medium">{rol}</span>
        <span class="font-medium">{message}</span>
    </div>
</div>